# Students API

Мащалкина Мария Сергеевна
Группа 5132704/30801

## 🚀 Быстрый старт
1. **Клонируйте репозиторий**
```bash
git clone <repository-url>
cd students_api
```

2. **Настройте переменные окружения**
```bash
cp .env.example .env
```

3. **Запустите приложение**
```bash
docker-compose up --build
```

4. **Проверьте работоспособность**
- API: http://localhost:8000
- Документация Swagger: http://localhost:8000/docs
- Документация ReDoc: http://localhost:8000/redoc
- Проверка здоровья: http://localhost:8000/health

### Остановка
```bash
docker-compose down -v  
# или
docker-compose down     # без удаления данных
```

## 📊 API Эндпоинты

Базовый URL: `http://localhost:8000/api/v1`

### 🎓 Студенты
| Метод | Эндпоинт | Описание |
|-------|----------|----------|
| `POST` | `/students` | Создать нового студента |
| `GET` | `/students` | Получить список всех студентов |
| `GET` | `/students/{id}` | Получить студента по ID |
| `PUT` | `/students/{id}` | Обновить данные студента |
| `DELETE` | `/students/{id}` | Удалить студента |

### 👥 Группы
| Метод | Эндпоинт | Описание |
|-------|----------|----------|
| `POST` | `/groups` | Создать новую группу |
| `GET` | `/groups` | Получить список всех групп |
| `GET` | `/groups/{id}` | Получить группу по ID |
| `PUT` | `/groups/{id}` | Обновить данные группы |
| `DELETE` | `/groups/{id}` | Удалить группу |
| `GET` | `/groups/{id}/students` | Получить студентов в группе |

### 🔄 Операции с группами
| Метод | Эндпоинт | Описание |
|-------|----------|----------|
| `POST` | `/students/{id}/groups/{group_id}` | Добавить студента в группу |
| `DELETE` | `/students/{id}/groups` | Удалить студента из группы |
| `PUT` | `/students/{id}/transfer/{new_group_id}` | Перевести студента в другую группу |

## 🏗️ Структура проекта

```
students_api/
├── .env.example           # Шаблон переменных окружения
├── docker-compose.yml     # Конфигурация Docker Compose
├── requirements.txt       # Зависимости Python
├── README.md              # Документация
├── init.sql              # Скрипт инициализации БД
│
├── app/                   # Основное приложение
│   ├── main.py           # Точка входа FastAPI
│   ├── config.py         # Конфигурация приложения
│   │
│   ├── api/              # Слой API
│   │   └── v1/
│   │       ├── students.py
│   │       └── groups.py
│   │
│   ├── core/             # Ядро приложения
│   │   ├── database.py   # Подключение к БД
│   │   └── models.py     # SQLAlchemy модели
│   │
│   ├── schemas/          # Pydantic схемы
│   │   ├── student.py
│   │   └── group.py
│   │
│   └── services/         # Бизнес-логика
│       ├── student_service.py
│       └── group_service.py
```

## 🛠️ Технологии

- **FastAPI 0.115.5** — Современный асинхронный веб-фреймворк
- **SQLAlchemy 2.0.36** — ORM для работы с базами данных
- **PostgreSQL 15** — Реляционная база данных
- **Pydantic 2.10.3** — Валидация и сериализация данных
- **asyncpg 0.30.0** — Асинхронный драйвер PostgreSQL
- **Docker & Docker Compose** — Контейнеризация и оркестрация


## 🔍 Тестирование API

### Интерактивная документация
- **Swagger UI**: http://localhost:8000/docs

### Проверка работоспособности
```bash
# Проверить здоровье сервиса
curl http://localhost:8000/health

# Получить информацию о сервисе
curl http://localhost:8000/
```


### Управление Docker
```bash
# Запуск в фоновом режиме
docker-compose up -d

# Остановка всех сервисов
docker-compose down

# Пересборка образов
docker-compose build

# Просмотр логов в реальном времени
docker-compose logs -f app
```


